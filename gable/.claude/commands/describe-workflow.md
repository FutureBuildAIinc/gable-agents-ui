---
# SPDX-License-Identifier: LicenseRef-OpenLBM-Docs-1.0
# SPDX-FileCopyrightText: 2026 FutureBuild, Inc. and OpenLBM contributors
description: Capture how a real lumberyard job works as a buildable spec with acceptance criteria — no coding required.
argument-hint: [the workflow: will-call, dispatch, quoting, yard picking, POs...]
---

Use the **describe-a-workflow** skill.

Workflow to capture: $ARGUMENTS

First orient yourself in what Gable already does so you can ask "what is different about yours?" rather than "how does this work?". Then interview, then write the spec to `docs/workflows/<name>.md` with all three kinds of acceptance criteria. You are the scribe — do not redesign their yard.
