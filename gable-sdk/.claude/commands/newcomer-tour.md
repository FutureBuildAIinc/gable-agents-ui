---
# SPDX-License-Identifier: LicenseRef-OpenLBM-Docs-1.0
# SPDX-FileCopyrightText: 2026 FutureBuild, Inc. and OpenLBM contributors
description: Get oriented in the Module SDK — the apps package, the Manifest/Registry/Handler model, and the three host ports.
argument-hint: [optional: a type, file, or question]
---

Use the **explain-this-code** skill.

Area of interest: $ARGUMENTS

Read `go doc ./apps` and `apps/doc.go` before answering — the design is written down there. Show real paths and line numbers, quote the invariants where relevant, and end by teaching the lookup (`go doc ./apps <Type>`).
